spring:
  datasource:
    url: jdbc:mysql://localhost:3306/carrentaltestdb
    username: root
    password: Dolphin880
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true